/*
 * Entity sprite layers.
 * 
 * Author: ANybakk
 */

namespace ANybakk {

  namespace EntitySpriteLayer {


    /**
     * Enumeration for a sprite's layer (z-index)
     */
    enum Orientation {
    
      LAYER_ABYSS = -500,
      LAYER_BACKGROUND = -50,
      LAYER_LADDER = -40,
      LAYER_DEFAULT = 0,
      LAYER_FOREGROUND = 50,
      LAYER_MECHANISM_FOREGROUND = 500
      
    }
    
  }
  
}